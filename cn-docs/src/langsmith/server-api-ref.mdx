---
title: LangSmith 部署的 Agent Server API 参考
sidebarTitle: Overview
---
Agent Server API 参考文档可在每个[部署](/langsmith/deployments)的 `/docs` 端点获取（例如 `http://localhost:8124/docs`）。

您可以在侧边栏的 **Agent Server API** 部分浏览完整的 API 参考，或查看下面的端点分组：

- [助手](/langsmith/agent-server-api/assistants) - 已配置的图实例
- [线程](/langsmith/agent-server-api/threads) - 一组运行（runs）的累积输出
- [线程运行](/langsmith/agent-server-api/thread-runs) - 在某个线程上对图/助手的调用
- [无状态运行](/langsmith/agent-server-api/stateless-runs) - 无状态持久化的调用
- [定时任务](/langsmith/agent-server-api/crons-plus-tier) - 按计划定期运行
- [存储](/langsmith/agent-server-api/store) - 用于长期记忆的持久化键值存储
- [A2A](/langsmith/agent-server-api/a2a) - 智能体间协议端点
- [MCP](/langsmith/agent-server-api/mcp) - 模型上下文协议端点
- [系统](/langsmith/agent-server-api/system) - 健康检查和服务器信息

## 身份验证

对于部署到 LangSmith 的情况，需要进行身份验证。向 Agent Server 发送的每个请求都需要传递 `X-Api-Key` 请求头。该请求头的值应设置为部署 Agent Server 的组织的一个有效的 LangSmith API 密钥。

`curl` 命令示例：

```shell
curl --request POST \
  --url http://localhost:8124/assistants/search \
  --header 'Content-Type: application/json' \
  --header 'X-Api-Key: LANGSMITH_API_KEY' \
  --data '{
  "metadata": {},
  "limit": 10,
  "offset": 0
}'
```
