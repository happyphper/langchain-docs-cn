---
title: (MODULE_NAME)
---
如何使用此 Python 工具模板：

- [ ] 将 (MODULE_NAME) 替换为工具的名称（可以使用 `command + F` 查找替换）
- [ ] 更新链接，使其指向正确的工具
- [ ] 在详情和功能表格下，更新 ✅/❌ 以反映工具的实际能力
- [ ] 如有需要，更新 PyPi/registry 包名
- [ ] 如有需要，更新 API 密钥环境变量名

模板从此行下方开始...

本指南提供了快速入门 (MODULE_NAME) [工具](/oss/langchain/tools) 的概述。如需了解所有 (MODULE_NAME) 功能、参数和配置的详细列表，请前往 [(MODULE_NAME) API 参考](<LINK>)。

## 概述

### 详情

| 类 | 包 | 可序列化 | [JS 支持](https://js.langchain.com/docs/integrations/chat/anthropic) | 下载量 | 版本 |
| :--- | :--- | :---: | :---: |  :---: | :---: | :---: |
| [(MODULE_NAME)](<LINK>) | [langchain-(MODULE_NAME)](<LINK>) | beta/❌ | ✅/❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-(MODULE_NAME)?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-(MODULE_NAME)?style=flat-square&label=%20) |

### 功能

- 在此处列出功能。如果需要，可以添加图标！

---

## 设置

要使用 (MODULE_NAME) 工具，您需要创建一个 (MODULE_NAME) 账户，获取 API 密钥，并安装 `langchain-(MODULE_NAME)` 集成包。

### 凭证

```python Set API key icon="key"
import getpass
import os

if "(MODULE_NAME)_API_KEY" not in os.environ:
    os.environ["(MODULE_NAME)_API_KEY"] = getpass.getpass("Enter your (MODULE_NAME) API key: ")
```



设置 LangSmith 也很有帮助（但不是必需的），以便对您的工具调用进行一流的可观测性/<Tooltip tip="记录模型执行的每个步骤以进行调试和改进">追踪</Tooltip>。要启用自动追踪，请设置您的 [LangSmith](https://docs.langchain.com/langsmith/home) API 密钥：

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### 安装

(MODULE_NAME) 工具位于 `langchain-(MODULE_NAME)` 包中：

<CodeGroup>
    ```python pip
    pip install -U langchain-(MODULE_NAME)
    ```
    ```python uv
    uv add langchain-(MODULE_NAME)
    ```
</CodeGroup>

---

## 实例化

现在我们可以实例化一个 (MODULE_NAME) 工具：

```python Initialize tool instance icon="robot"
from langchain_(MODULE_NAME) import (MODULE_NAME)

tool = (MODULE_NAME)(...)
```

---

## 调用

### 直接调用

TODO: 描述工具参数是什么，并填写它们

```python Call tool icon="rocket"
tool.invoke({...})
```

### 作为 ToolCall 调用

我们也可以使用模型生成的 `ToolCall` 来调用该工具，在这种情况下将返回一个 @[`ToolMessage`]：

```python ToolCall icon="toolbox"
# 这通常由模型生成，但为了演示目的，我们将直接创建一个工具调用。
model_generated_tool_call = {
    "args": {...},  # TODO: 填写
    "id": "1",
    "name": tool.name,
    "type": "tool_call",
}
tool.invoke(model_generated_tool_call)
```

### 在智能体中使用

我们可以在智能体中使用我们的工具。为此，我们需要一个具备工具调用能力的模型。

```python Agent with tool icon="robot"
# 调用模型需要 pip install -qU "langchain[anthropic]"
from langchain.agents import create_agent

tools = [tool]
agent = create_agent(
    model="claude-sonnet-4-5-20250929",
    tools=tools,
)

# 运行智能体
agent.invoke(
    {"messages": [{"role": "user", "content": "INSERT QUERY"}]}
)
```

---

## TODO: 此工具特有的任何功能

如果不相关，请删除。

查看现有工具文档以获取示例，例如：

- [MCP Toolbox](/oss/integrations/tools/toolbox)
- [Discord](/oss/integrations/tools/discord)
- [DuckDuckGo Search](/oss/integrations/tools/ddg)

---

## API 参考

有关所有 (MODULE_NAME) 功能和配置的详细文档，请前往 [API 参考](<LINK>)。
